- [ ] `prod-stage` workflow completes successfully on a QA-approved RC (explicit `-f version=...` or empty input)
- [ ] `check` job enforces: environment variables defined, resolved version is QA-approved (explicit path: RC exists + QA approved; empty path: latest `*-qa-approved` tag), version not yet released, artifacts resolved
- [ ] `prod-stage` workflow fails at the "Check QA Approved" step when an explicit un-approved RC was passed
- [ ] `run` job is skipped when `check` fails — no deployment, no release version tag
- [ ] Re-running with the same RC a second time fails at "Verify Version Not Yet Released"
- [ ] Release version tag (e.g. `v2.5.0`) is applied to each component image alongside the RC tag
- [ ] GitHub Release exists for the release version and is marked as Latest
- [ ] Per-component git tags are pushed for every component VERSION file
- [ ] For multi-component systems, every component is listed in `Resolve Artifacts`, `Deploy System`, and `Create Component Tags`
- [ ] Deployment mechanics are shared with the QA Stage — only environment and release-version tagging differ
