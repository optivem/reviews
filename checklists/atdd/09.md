**Task 1: WireMock Docker Setup**

- [ ] `ExternalSystemMode` enum is defined with `REAL` and `STUB` values
- [ ] Mode is carried through `Configuration` to `UseCaseContext`
- [ ] WireMock services are defined for each external system in the stub docker-compose file
- [ ] Each WireMock stub has a health check mapping
- [ ] `docker compose up` starts successfully and health endpoints respond


**Task 2: Stub Clients and Drivers**

- [ ] Each external system has a stub client with a `configure*()` method backed by WireMock
- [ ] Each external system has a stub driver that implements the same interface as the real driver
- [ ] `ClockDriver` interface and real/stub drivers are added (Clock had no driver previously)
- [ ] `BaseConfigurableTest` reads `ExternalSystemMode` from environment variable
- [ ] Creates real or stub drivers based on the mode
- [ ] All test base classes extend `BaseConfigurableTest`
- [ ] Project builds successfully


**Task 3: Smoke Tests in Both Modes**

- [ ] `ClockSmokeTest` added using `scenario.assume().clock().shouldBeRunning()`
- [ ] All smoke tests pass in `STUB` mode
- [ ] All smoke tests pass in `REAL` mode
