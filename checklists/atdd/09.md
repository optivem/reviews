**Milestone 1: External System Smoke Tests — Real HTTP**
- [ ] `ExternalSystemMode` enum is defined with `REAL` and `STUB` values
- [ ] `ExternalSystemMode` is carried through `AppConfiguration` to `UseCaseContext`
- [ ] `BaseConfigurableTest` reads `EXTERNAL_SYSTEM_MODE` from an environment variable
- [ ] `BaseConfigurableTest` creates real HTTP drivers for all HTTP external systems
- [ ] All test base classes extend `BaseConfigurableTest`
- [ ] All smoke tests pass with `EXTERNAL_SYSTEM_MODE=REAL`

**Milestone 2: External Systems — Real Non-IO**
- [ ] `ClockDriver` interface is defined with `goToClock()`, `getTime()`, and `returnsTime()` methods
- [ ] `ClockRealDriver` implements `ClockDriver` with no HTTP client in its constructor
- [ ] `returnsTime()` in `ClockRealDriver` is a no-op (real clock cannot be configured)
- [ ] `ClockDriver` is registered in `BaseConfigurableTest` using `ClockRealDriver`

**Milestone 3: External System Smoke Tests — Real Non-IO**
- [ ] `ClockSmokeTest` uses `scenario.assume().clock().shouldBeRunning()`
- [ ] `ClockSmokeTest` passes with `EXTERNAL_SYSTEM_MODE=REAL`

**Milestone 4: External Systems — Stub**
- [ ] WireMock Docker service is defined for each external system in the stub docker-compose file
- [ ] Each WireMock stub has a health check mapping
- [ ] Each external system has a stub client with a `configure*()` method backed by WireMock
- [ ] Each external system has a stub driver implementing the same interface as the real driver
- [ ] `BaseConfigurableTest` switches between real and stub drivers based on `ExternalSystemMode`

**Milestone 5: External System Smoke Tests — Stub**
- [ ] All smoke tests pass in STUB mode
- [ ] All smoke tests pass in REAL mode

**Milestone 6: Acceptance Stage**
- [ ] Smoke tests run in Acceptance Stage in both STUB and REAL modes
