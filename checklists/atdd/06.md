**Milestone 1: Channels**
- [ ] `ChannelType` is defined with `API` and `UI` values
- [ ] `optivem-testing` dependency is added

**Milestone 2: System Smoke Tests**
- [ ] Shop Smoke Test uses the channel mechanism (no base class or API/UI subclasses)

**Milestone 3: E2E Tests**
- [ ] Each E2E test uses the channel mechanism (no base class or API/UI subclasses)
- [ ] Each E2E test runs on both channels

**Milestone 4: Acceptance Stage**
- [ ] Smoke tests run in the Acceptance Stage for API and UI channels
- [ ] E2E tests run in the Acceptance Stage for API and UI channels
