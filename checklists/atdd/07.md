**Task 1: System DSL + System Smoke Tests**

- [ ] `UseCase` interface is defined
- [ ] `BaseUseCase` holds driver and context
- [ ] `UseCaseContext` manages param and result alias maps
- [ ] `VoidVerification`, `ResponseVerification`, and `ErrorVerification` are defined
- [ ] `UseCaseDsl` is the entry point with one accessor per domain, each created lazily
- [ ] `ShopDsl` has one factory method per use case; data-returning use cases have a paired `*Verification` class
- [ ] `BaseUseCaseDslTest` exposes `app` and no longer exposes raw drivers
- [ ] All system smoke tests use `app.shop().goToShop().execute().shouldSucceed()` style
- [ ] System smoke tests pass


**Task 2: External System DSLs + External Smoke Tests**

- [ ] Each external system has a DSL class following the same pattern as `ShopDsl`
- [ ] All external system smoke tests use `app.erp().goToErp().execute().shouldSucceed()` style
- [ ] All smoke tests pass (both system and external)


**Task 3: E2E Tests**

- [ ] All E2E tests use DSL use case calls with no manual request construction
- [ ] Aliases are used to thread system-generated values between use cases
- [ ] All E2E tests pass
